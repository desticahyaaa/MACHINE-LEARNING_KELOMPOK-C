from xgboost import XGBRegressor
from sklearn.metrics import mean_squared_error, r2_score, mean_absolute_error
import numpy as np
# No need to import train_test_split as it's already done in previous cells

# X and y, and X_train, X_test, y_train, y_test are already defined
# and split in previous cells (GEgCGZBePoME and VtBGTfsIZzhv).
# We will use the pre-defined X_train, y_train, etc. from those steps.

# Initialize the XGBoost Regressor model
xgb_model = XGBRegressor(random_state=42)

# Train the model using the training data
print("\nTraining XGBoost Regressor...")
xgb_model.fit(X_train, y_train)

# Make predictions on the test set using the XGBoost model
y_pred_xgb = xgb_model.predict(X_test)

print("\n--- XGBoost Regressor Performance ---")
# Calculate R-squared for XGBoost
r_squared_xgb = r2_score(y_test, y_pred_xgb)
rmse_xgb = np.sqrt(mean_squared_error(y_test, y_pred_xgb))
mse_xgb = mean_squared_error(y_test, y_pred_xgb)
mae_xgb = mean_absolute_error(y_test, y_pred_xgb)

print(f"XGBoost R-squared: {r_squared_xgb:.4f}")
print(f"XGBoost RMSE: {rmse_xgb:.2f}")
print(f"XGBoost MSE: {mse_xgb:.2f}")
print(f"XGBoost MAE: {mae_xgb:.2f}")

print("\nFeature Importances:")
feature_importances_xgb = pd.DataFrame({'Feature': X.columns, 'Importance': xgb_model.feature_importances_})
display(feature_importances_xgb.sort_values(by='Importance', ascending=False))

import matplotlib.pyplot as plt
import seaborn as sns

plt.figure(figsize=(10, 7))
sns.scatterplot(x=y_test, y=y_pred_xgb, alpha=0.6)
plt.plot([y_test.min(), y_test.max()], [y_test.min(), y_test.max()], 'r--', lw=2) # Line for perfect prediction
plt.xlabel('Actual Prices')
plt.ylabel('Predicted Prices')
plt.title('Actual vs. Predicted Prices (XGBoost Regressor)')
plt.grid(True, linestyle='--', alpha=0.7)
plt.show()

# Tabel perbandingan
results_df = pd.DataFrame({
    'Model': ['Ridge Regression', 'Lasso Regression', 'Random Forest Regressor', 'XGBoost Regressor'],
    'R-squared': [r_squared_ridge, r_squared_lasso, r_squared_rf, r_squared_xgb],
    'RMSE': [rmse_ridge, rmse_lasso, rmse_rf, rmse_xgb],
    'MSE': [mse_ridge, mse_lasso, mse_rf, mse_xgb],
    'MAE': [mae_ridge, mae_lasso, mae_rf, mae_xgb]
})

# Display the comparison table
display(results_df.sort_values(by='R-squared', ascending=False))
